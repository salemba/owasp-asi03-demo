# Contributing

## Workflow

- Use Conventional Commits (for example: `feat(gateway): add health endpoint`).
- Run `make lint typecheck test` before pushing.
- Run `pre-commit install` once and ensure hooks pass.

## Scope

- This scaffold intentionally excludes business logic and exploit implementation.
- Use `# TODO(step-N)` markers when adding future teaching scenarios.
